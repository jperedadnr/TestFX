---
name: Build

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

permissions:
  actions: read
  checks: write
  contents: read
  issues: read
  packages: read
  pull-requests: write
  statuses: read
  security-events: write

jobs:
  build:
    strategy:
      matrix:
        os: [ macos-latest, ubuntu-latest ]
        include:
          - os: macos-latest
            FILE: openjfx-24+77_headless_mac-aarch64_bin-sdk
          - os: ubuntu-latest
            FILE: openjfx-24+77_headless_linux-x86_64_bin-sdk

    runs-on: ${{ matrix.os }}
    timeout-minutes: 20

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: 22
          distribution: 'temurin'

      - name: Download SDK (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          wget -nv https://download2.gluonhq.com/openjfx/forks/johan/headless/${{ matrix.FILE }}.zip -O ${{ matrix.FILE }}.zip
          unzip -q ${{ matrix.FILE }}.zip
          echo "JAVAFX_HOME=${{ github.workspace }}/javafx-sdk-24" >> ${GITHUB_ENV}

      - name: Download SDK (Windows)
        if: runner.os == 'Windows'
        run: |
          Invoke-WebRequest https://download2.gluonhq.com/openjfx/forks/johan/headless/${{ matrix.FILE }}.zip -OutFile ${{ matrix.FILE }}.zip
          Expand-Archive .\${{ matrix.FILE }}.zip -Force
          Move-Item -Path .\${{ matrix.FILE }}\javafx-sdk-24 -Destination .\
          echo "JAVAFX_HOME=${{ github.workspace }}\javafx-sdk-24" >> %GITHUB_ENV%

      - name: Cache Gradle Resources
        uses: actions/cache@v4
        with:
          path: ~/.gradle
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}
          restore-keys: ${{ runner.os }}-gradle

      - name: Set up Virtual Display (for Linux)
        if: runner.os == 'Linux'
        run: |
          export DISPLAY=:99
          Xvfb -ac :99 -screen 0 1920x1080x24 > /dev/null 2>&1 &

      - name: Build (Linux/macOS)
        if: runner.os != 'Windows'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          export _JAVA_TOOL_OPTIONS='-Dtestfx.headless=true -Dtestfx.verbose=true -Dprism.verbose=true'
          ./gradlew versions check

      - name: Build (Windows)
        if: runner.os == 'Windows'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set _JAVA_OPTIONS=-Dtestfx.headless=true
          .\gradlew versions check

      - name: Publish Test Results
        uses: EnricoMi/publish-unit-test-result-action/composite@v2
        if: always()
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          check_name: Unit Test Results (${{ inputs.os }}-java-${{ inputs.jvm }})
          report_individual_runs: true
          check_run_annotations_branch: master
          check_run_annotations: all tests, skipped tests
          files: subprojects/**/build/test-results/**/*.xml
